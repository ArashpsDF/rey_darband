<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>منوی آنلاین سرای سنتی</title>
    
    <!-- فونت وزیرمتن -->
    <link href="https://cdn.jsdelivr.net/gh/rastikerdar/vazirmatn@v33.0.0/Vazirmatn-font-face.css" rel="stylesheet" type="text/css" />
    <!-- آیکون‌ها -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- تیلویند -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Vazirmatn', 'sans-serif'],
                    },
                    colors: {
                        persian: {
                            teal: '#139b9b',     /* فیروزه ای */
                            tealDark: '#0e7a7a',
                            cream: '#fcfaf5',    /* کرم/استخوانی */
                            creamDark: '#f0ebd8',
                            crimson: '#9e1a32',  /* زرشکی/لعل */
                            gold: '#d4af37'      /* طلایی برای المان‌های لوکس */
                        }
                    }
                }
            }
        }
    </script>

    <style>
        body { background-color: #fcfaf5; }
        
        /* پترن اسلیمی ظریف برای پس زمینه هدر */
        .bg-pattern {
            background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23139b9b' fill-opacity='0.08'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
        }

        /* اسکرول‌بار اختصاصی */
        ::-webkit-scrollbar { width: 6px; height: 4px; }
        ::-webkit-scrollbar-track { background: #f0ebd8; }
        ::-webkit-scrollbar-thumb { background: #139b9b; border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: #0e7a7a; }

        .hide { display: none !important; }
        .glass-panel {
            background: rgba(252, 250, 245, 0.95);
            backdrop-filter: blur(10px);
        }
    </style>
</head>
<body class="text-gray-800 antialiased relative pb-20">

    <!-- ==================== بخش مشتری (Customer View) ==================== -->
    <div id="customer-view" class="max-w-4xl mx-auto min-h-screen shadow-2xl bg-white border-x border-persian-creamDark relative">
        
        <!-- هدر و اطلاعات مجموعه -->
        <header class="bg-pattern border-b-4 border-persian-teal relative pt-8 pb-12 px-4 text-center rounded-b-[40px] shadow-md">
            <div class="absolute top-4 left-4 text-persian-teal opacity-50 cursor-pointer" id="admin-trigger" title="ورود مدیریت (دوبار کلیک)">
                <i class="fas fa-lock"></i>
            </div>
            
            <img id="cv-logo" src="https://placehold.co/150x150/139b9b/ffffff?text=Logo" alt="لوگو" class="w-28 h-28 mx-auto rounded-full border-4 border-persian-gold shadow-lg object-cover mb-4">
            <h1 id="cv-name" class="text-3xl font-bold text-persian-crimson mb-2">سرای سنتی ایرانی</h1>
            <p id="cv-desc" class="text-sm text-gray-600 mb-6 px-4">محیطی دنج و آرام برای ثبت خاطرات به‌یادماندنی</p>
            
            <div class="flex flex-wrap justify-center gap-4 text-sm font-medium">
                <a id="cv-phone-link" href="#" class="bg-white px-4 py-2 rounded-full shadow border border-persian-tealDark text-persian-tealDark flex items-center gap-2 hover:bg-persian-teal hover:text-white transition">
                    <i class="fas fa-phone"></i> <span id="cv-phone">021-0000000</span>
                </a>
                <a id="cv-map" href="#" target="_blank" class="bg-white px-4 py-2 rounded-full shadow border border-persian-tealDark text-persian-tealDark flex items-center gap-2 hover:bg-persian-teal hover:text-white transition">
                    <i class="fas fa-map-marker-alt"></i> مسیریابی
                </a>
            </div>
        </header>

        <!-- دسته بندی منو (تب‌ها) -->
        <div class="sticky top-0 z-40 bg-white/95 backdrop-blur shadow-sm border-b border-gray-100">
            <ul id="cv-categories" class="flex overflow-x-auto gap-2 p-4 snap-x text-sm whitespace-nowrap scroll-smooth">
                <!-- دسته‌بندی‌ها با جاوااسکریپت پر می‌شوند -->
            </ul>
        </div>

        <!-- لیست آیتم‌های منو -->
        <main class="p-4 sm:p-6 pb-12">
            <h2 id="current-category-title" class="text-xl font-bold text-persian-teal mb-6 flex items-center gap-2">
                <i class="fas fa-utensils"></i> <span>همه موارد</span>
            </h2>
            <div id="cv-items-grid" class="grid grid-cols-1 sm:grid-cols-2 gap-6">
                <!-- آیتم‌ها اینجا قرار می‌گیرند -->
            </div>
        </main>

        <!-- فرم درخواست رزرو -->
        <section class="bg-persian-cream p-6 border-t-2 border-persian-gold mt-8">
            <h3 class="text-lg font-bold text-persian-crimson mb-4"><i class="fas fa-calendar-alt ml-2"></i>درخواست رزرو میز/آلاچیق</h3>
            <form id="reservation-form" class="space-y-4" onsubmit="submitReservation(event)">
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                    <input type="text" placeholder="نام و نام خانوادگی" required class="w-full p-3 rounded-lg border border-gray-300 focus:border-persian-teal focus:ring-1 focus:ring-persian-teal outline-none">
                    <input type="tel" placeholder="شماره تماس" required class="w-full p-3 rounded-lg border border-gray-300 focus:border-persian-teal outline-none">
                    <input type="number" placeholder="تعداد نفرات" min="1" required class="w-full p-3 rounded-lg border border-gray-300 focus:border-persian-teal outline-none">
                    <select class="w-full p-3 rounded-lg border border-gray-300 focus:border-persian-teal outline-none text-gray-600">
                        <option value="">نوع لژ را انتخاب کنید</option>
                        <option value="تخت سنتی">تخت سنتی</option>
                        <option value="آلاچیق">آلاچیق اختصاصی</option>
                        <option value="سالن VIP">سالن VIP</option>
                        <option value="میز عادی">میز عادی</option>
                    </select>
                </div>
                <div class="grid grid-cols-2 gap-4">
                    <input type="date" title="تاریخ رزرو" required class="w-full p-3 rounded-lg border border-gray-300 text-gray-600 focus:border-persian-teal outline-none">
                    <input type="time" title="ساعت رزرو" required class="w-full p-3 rounded-lg border border-gray-300 text-gray-600 focus:border-persian-teal outline-none">
                </div>
                <button type="submit" class="w-full bg-persian-teal text-white font-bold py-3 rounded-lg hover:bg-persian-tealDark transition shadow-md">
                    ارسال درخواست رزرو
                </button>
            </form>
        </section>

        <!-- فوتر -->
        <footer class="bg-gray-800 text-gray-300 text-center py-8 px-4 mt-8 rounded-t-[30px]">
            <p id="cv-footer-address" class="mb-2 text-sm"><i class="fas fa-map-pin ml-1"></i> آدرس: ...</p>
            <p id="cv-footer-hours" class="mb-4 text-sm"><i class="fas fa-clock ml-1"></i> ساعت کاری: ...</p>
            <p class="text-xs text-gray-500 mt-6 border-t border-gray-700 pt-4">© تمامی حقوق برای این مجموعه محفوظ است.</p>
        </footer>
    </div>


    <!-- ==================== بخش ورود مدیریت (Login Modal) ==================== -->
    <div id="login-modal" class="hide fixed inset-0 z-50 flex items-center justify-center bg-black/60 backdrop-blur-sm p-4">
        <div class="bg-white rounded-2xl shadow-2xl w-full max-w-sm overflow-hidden animate-fade-in">
            <div class="bg-persian-teal p-4 text-center">
                <h3 class="text-white font-bold text-lg"><i class="fas fa-user-shield ml-2"></i>ورود به پنل مدیریت</h3>
            </div>
            <div class="p-6 space-y-4">
                <div>
                    <label class="block text-sm text-gray-600 mb-1">نام کاربری</label>
                    <input type="text" id="login-user" class="w-full p-2 border rounded bg-gray-50 text-left dir-ltr focus:border-persian-teal outline-none" value="admin">
                </div>
                <div>
                    <label class="block text-sm text-gray-600 mb-1">رمز عبور</label>
                    <input type="password" id="login-pass" class="w-full p-2 border rounded bg-gray-50 text-left dir-ltr focus:border-persian-teal outline-none" value="admin">
                </div>
                <button onclick="handleLogin()" class="w-full bg-persian-teal text-white py-2 rounded font-bold hover:bg-persian-tealDark">ورود</button>
                <button onclick="closeLogin()" class="w-full bg-gray-200 text-gray-700 py-2 rounded hover:bg-gray-300">انصراف</button>
            </div>
        </div>
    </div>


    <!-- ==================== پنل مدیریت (Admin View) ==================== -->
    <div id="admin-view" class="hide fixed inset-0 z-50 bg-gray-100 overflow-y-auto">
        
        <!-- هدر مدیریت -->
        <header class="bg-gray-900 text-white p-4 sticky top-0 z-40 flex justify-between items-center shadow-md">
            <div class="flex items-center gap-3">
                <i class="fas fa-cogs text-xl text-persian-gold"></i>
                <h1 class="font-bold text-lg">پنل مدیریت منو</h1>
            </div>
            <div class="flex gap-2">
                <button onclick="saveToGithub()" class="bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded text-sm font-bold shadow-lg flex items-center gap-2 transition">
                    <i class="fas fa-cloud-upload-alt"></i> <span>ذخیره در سایت</span>
                </button>
                <button onclick="exitAdmin()" class="bg-red-600 hover:bg-red-700 text-white px-3 py-2 rounded text-sm transition">
                    <i class="fas fa-sign-out-alt"></i>
                </button>
            </div>
        </header>

        <div class="max-w-5xl mx-auto p-4 flex flex-col md:flex-row gap-6 mt-4">
            
            <!-- منوی سایدبار مدیریت -->
            <div class="w-full md:w-64 flex-shrink-0">
                <div class="bg-white rounded-xl shadow-sm overflow-hidden flex flex-col">
                    <button onclick="switchAdminTab('info')" class="admin-tab-btn active text-right p-4 border-b hover:bg-gray-50 font-medium text-persian-teal border-r-4 border-persian-teal" data-tab="info">
                        <i class="fas fa-info-circle w-6 text-center"></i> اطلاعات پایه
                    </button>
                    <button onclick="switchAdminTab('categories')" class="admin-tab-btn text-right p-4 border-b hover:bg-gray-50 text-gray-600 border-r-4 border-transparent" data-tab="categories">
                        <i class="fas fa-tags w-6 text-center"></i> دسته‌بندی‌ها
                    </button>
                    <button onclick="switchAdminTab('items')" class="admin-tab-btn text-right p-4 border-b hover:bg-gray-50 text-gray-600 border-r-4 border-transparent" data-tab="items">
                        <i class="fas fa-list w-6 text-center"></i> آیتم‌های منو
                    </button>
                    <button onclick="switchAdminTab('github')" class="admin-tab-btn text-right p-4 hover:bg-gray-50 text-gray-600 border-r-4 border-transparent" data-tab="github">
                        <i class="fab fa-github w-6 text-center"></i> تنظیمات گیت‌هاب
                    </button>
                </div>
            </div>

            <!-- محتوای تب‌ها -->
            <div class="flex-grow bg-white rounded-xl shadow-sm p-6 min-h-[500px]">
                
                <!-- تب اطلاعات پایه -->
                <div id="tab-info" class="admin-tab-content space-y-4">
                    <h2 class="text-xl font-bold mb-6 text-gray-800 border-b pb-2">اطلاعات پایه مجموعه</h2>
                    <div>
                        <label class="block text-sm text-gray-600 mb-1">نام مجموعه</label>
                        <input type="text" id="admin-name" class="w-full p-2 border rounded focus:border-persian-teal outline-none" onchange="updateInfo('name', this.value)">
                    </div>
                    <div>
                        <label class="block text-sm text-gray-600 mb-1">آدرس لوگو (URL)</label>
                        <input type="text" id="admin-logo" class="w-full p-2 border rounded text-left dir-ltr focus:border-persian-teal outline-none" onchange="updateInfo('logo', this.value)">
                    </div>
                    <div>
                        <label class="block text-sm text-gray-600 mb-1">شماره تلفن</label>
                        <input type="text" id="admin-phone" class="w-full p-2 border rounded focus:border-persian-teal outline-none" onchange="updateInfo('phone', this.value)">
                    </div>
                    <div>
                        <label class="block text-sm text-gray-600 mb-1">ساعت کاری</label>
                        <input type="text" id="admin-hours" class="w-full p-2 border rounded focus:border-persian-teal outline-none" onchange="updateInfo('hours', this.value)">
                    </div>
                    <div>
                        <label class="block text-sm text-gray-600 mb-1">آدرس پستی</label>
                        <textarea id="admin-address" class="w-full p-2 border rounded focus:border-persian-teal outline-none" onchange="updateInfo('address', this.value)"></textarea>
                    </div>
                    <div>
                        <label class="block text-sm text-gray-600 mb-1">لینک مسیریاب (Google Maps, Neshan...)</label>
                        <input type="text" id="admin-map" class="w-full p-2 border rounded text-left dir-ltr focus:border-persian-teal outline-none" onchange="updateInfo('mapLink', this.value)">
                    </div>
                </div>

                <!-- تب دسته‌بندی‌ها -->
                <div id="tab-categories" class="admin-tab-content hide space-y-4">
                    <h2 class="text-xl font-bold mb-6 text-gray-800 border-b pb-2">مدیریت دسته‌بندی‌ها</h2>
                    <div class="flex gap-2 mb-4">
                        <input type="text" id="new-cat-name" placeholder="نام دسته جدید..." class="flex-grow p-2 border rounded outline-none focus:border-persian-teal">
                        <button onclick="addCategory()" class="bg-persian-teal text-white px-4 rounded hover:bg-persian-tealDark">افزودن</button>
                    </div>
                    <ul id="admin-cat-list" class="space-y-2">
                        <!-- لیست دسته‌ها -->
                    </ul>
                </div>

                <!-- تب آیتم‌های منو -->
                <div id="tab-items" class="admin-tab-content hide">
                    <div class="flex justify-between items-center mb-6 border-b pb-2">
                        <h2 class="text-xl font-bold text-gray-800">مدیریت آیتم‌های منو</h2>
                        <button onclick="openItemModal()" class="bg-persian-teal text-white px-3 py-1 rounded text-sm hover:bg-persian-tealDark flex items-center gap-1">
                            <i class="fas fa-plus"></i> آیتم جدید
                        </button>
                    </div>
                    
                    <div class="overflow-x-auto">
                        <table class="w-full text-sm text-right text-gray-500">
                            <thead class="text-xs text-gray-700 uppercase bg-gray-50">
                                <tr>
                                    <th class="px-4 py-3">تصویر</th>
                                    <th class="px-4 py-3">نام</th>
                                    <th class="px-4 py-3">دسته</th>
                                    <th class="px-4 py-3">قیمت (تومان)</th>
                                    <th class="px-4 py-3">موجودی</th>
                                    <th class="px-4 py-3 text-center">عملیات</th>
                                </tr>
                            </thead>
                            <tbody id="admin-items-list">
                                <!-- لیست آیتم‌ها -->
                            </tbody>
                        </table>
                    </div>
                </div>

                <!-- تب تنظیمات گیت‌هاب -->
                <div id="tab-github" class="admin-tab-content hide space-y-4">
                    <h2 class="text-xl font-bold mb-2 text-gray-800 border-b pb-2">اتصال به گیت‌هاب (GitHub API)</h2>
                    <p class="text-xs text-gray-500 mb-6 bg-yellow-50 p-3 rounded border border-yellow-200">
                        <i class="fas fa-exclamation-triangle text-yellow-500 ml-1"></i>
                        برای اینکه تغییرات روی سایت اصلی اعمال شود، باید اطلاعات اکانت گیت‌هاب خود را اینجا وارد کنید. این اطلاعات فقط در مرورگر شما ذخیره می‌شود.
                    </p>
                    <div>
                        <label class="block text-sm text-gray-600 mb-1">GitHub Username (نام کاربری)</label>
                        <input type="text" id="gh-user" class="w-full p-2 border rounded bg-gray-50 text-left dir-ltr" placeholder="e.g. johndoe">
                    </div>
                    <div>
                        <label class="block text-sm text-gray-600 mb-1">Repository Name (نام ریپازیتوری)</label>
                        <input type="text" id="gh-repo" class="w-full p-2 border rounded bg-gray-50 text-left dir-ltr" placeholder="e.g. persian-menu">
                    </div>
                    <div>
                        <label class="block text-sm text-gray-600 mb-1">Personal Access Token (PAT)</label>
                        <input type="password" id="gh-pat" class="w-full p-2 border rounded bg-gray-50 text-left dir-ltr" placeholder="ghp_xxxxxxxxxxxxxxxxx">
                        <p class="text-[10px] text-gray-400 mt-1">باید دسترسی `repo` برای آپدیت فایل‌ها داشته باشد.</p>
                    </div>
                    <button onclick="saveGithubSettings()" class="bg-gray-800 text-white px-4 py-2 rounded mt-2 hover:bg-gray-700">
                        ذخیره تنظیمات گیت‌هاب در مرورگر
                    </button>
                </div>

            </div>
        </div>
    </div>

    <!-- پاپ‌آپ افزودن/ویرایش آیتم -->
    <div id="item-modal" class="hide fixed inset-0 z-[60] flex items-center justify-center bg-black/60 backdrop-blur-sm p-4">
        <div class="bg-white rounded-xl shadow-2xl w-full max-w-lg overflow-hidden max-h-[90vh] flex flex-col">
            <div class="bg-gray-100 p-4 border-b flex justify-between items-center">
                <h3 id="item-modal-title" class="font-bold text-gray-800">افزودن آیتم جدید</h3>
                <button onclick="closeItemModal()" class="text-gray-500 hover:text-red-500"><i class="fas fa-times text-xl"></i></button>
            </div>
            <div class="p-6 overflow-y-auto space-y-4 flex-grow">
                <input type="hidden" id="edit-item-id">
                <div>
                    <label class="block text-sm text-gray-600 mb-1">نام آیتم</label>
                    <input type="text" id="edit-item-name" class="w-full p-2 border rounded outline-none focus:border-persian-teal">
                </div>
                <div>
                    <label class="block text-sm text-gray-600 mb-1">دسته‌بندی</label>
                    <select id="edit-item-cat" class="w-full p-2 border rounded outline-none focus:border-persian-teal"></select>
                </div>
                <div>
                    <label class="block text-sm text-gray-600 mb-1">قیمت (تومان)</label>
                    <input type="number" id="edit-item-price" class="w-full p-2 border rounded text-left dir-ltr outline-none focus:border-persian-teal">
                </div>
                <div>
                    <label class="block text-sm text-gray-600 mb-1">توضیحات / ترکیبات</label>
                    <textarea id="edit-item-desc" class="w-full p-2 border rounded outline-none focus:border-persian-teal" rows="2"></textarea>
                </div>
                <div>
                    <label class="block text-sm text-gray-600 mb-1">آدرس تصویر (URL)</label>
                    <input type="text" id="edit-item-image" class="w-full p-2 border rounded text-left dir-ltr outline-none focus:border-persian-teal" placeholder="https://...">
                </div>
                <div>
                    <label class="block text-sm text-gray-600 mb-1">برچسب ویژه (اختیاری)</label>
                    <input type="text" id="edit-item-tag" class="w-full p-2 border rounded outline-none focus:border-persian-teal" placeholder="مثال: پیشنهاد سرآشپز، تند">
                </div>
                <div class="flex items-center gap-2 mt-2">
                    <input type="checkbox" id="edit-item-stock" class="w-4 h-4 text-persian-teal" checked>
                    <label for="edit-item-stock" class="text-sm text-gray-700 cursor-pointer">موجود است</label>
                </div>
            </div>
            <div class="p-4 bg-gray-50 border-t flex gap-2">
                <button onclick="saveItem()" class="flex-grow bg-persian-teal text-white py-2 rounded hover:bg-persian-tealDark">ذخیره آیتم</button>
                <button onclick="closeItemModal()" class="px-4 bg-gray-200 text-gray-700 rounded hover:bg-gray-300">لغو</button>
            </div>
        </div>
    </div>


    <!-- ==================== اسکریپت‌ها (JavaScript) ==================== -->
    <script>
        // دیتای اولیه پیش‌فرض در صورتی که فایلی از گیت‌هاب لود نشود
        const INITIAL_DATA = {
            restaurant: {
                name: "سرای سنتی حافظ",
                logo: "https://images.unsplash.com/photo-1541592106381-b31e9677c0e5?auto=format&fit=crop&q=80&w=200",
                address: "تهران، خیابان ولیعصر، نرسیده به تجریش، کوچه باغ، پلاک ۱",
                phone: "021-22334455",
                hours: "هر روز از ساعت ۱۰:۰۰ صبح الی ۲۳:۳۰ شب",
                mapLink: "https://maps.google.com"
            },
            categories: [
                { id: "cat-1", name: "غذاهای سنتی" },
                { id: "cat-2", name: "پیش غذا و مخلفات" },
                { id: "cat-3", name: "قلیان‌های ویژه" },
                { id: "cat-4", name: "دمنوش و نوشیدنی" }
            ],
            items: [
                { id: "item-1", categoryId: "cat-1", name: "دیزی سنگی مخصوص", desc: "گوشت تازه گوسفندی، نخود، سیب‌زمینی، دنبه، همراه با نان سنگک تازه و ترشی", price: 280000, image: "https://images.unsplash.com/photo-1604908176997-125f25cc6f3d?auto=format&fit=crop&q=80&w=400", tags: "پیشنهاد سرآشپز", inStock: true },
                { id: "item-2", categoryId: "cat-1", name: "چلو کباب کوبیده", desc: "دو سیخ کباب ۱۲۰ گرمی، برنج صدری ایرانی، گوجه کبابی", price: 320000, image: "https://images.unsplash.com/photo-1628294895950-9805252327bc?auto=format&fit=crop&q=80&w=400", tags: "", inStock: true },
                { id: "item-3", categoryId: "cat-2", name: "کشک بادمجان", desc: "بادمجان کبابی، کشک محلی، گردو و نعنا داغ فراوان", price: 140000, image: "https://images.unsplash.com/photo-1577906096429-f73c2c312435?auto=format&fit=crop&q=80&w=400", tags: "محبوب", inStock: true },
                { id: "item-4", categoryId: "cat-3", name: "قلیان عربی دو سیب", desc: "تنباکوی مرغوب، یخ خشک، سرویس چای و باقلوا", price: 350000, image: "https://images.unsplash.com/photo-1551601651-2a8555f1a136?auto=format&fit=crop&q=80&w=400", tags: "", inStock: false },
                { id: "item-5", categoryId: "cat-4", name: "دمنوش آرامش", desc: "گل گاوزبان، سنبل الطیب، لیمو عمانی و نبات زعفرانی", price: 850000, image: "https://images.unsplash.com/photo-1576092768241-dec231879fc3?auto=format&fit=crop&q=80&w=400", tags: "", inStock: true }
            ]
        };

        // متغیر گلوبال برای نگهداری دیتای برنامه
        let appData = null;
        let currentCategory = 'all';

        // فرمت کردن عدد به قیمت (سه رقم سه رقم)
        const formatPrice = (num) => num.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
        
        // تولید ID یکتا
        const generateId = () => Math.random().toString(36).substr(2, 9);

        // انکود و دیکود Base64 برای پشتیبانی از حروف فارسی (UTF-8)
        const utf8_to_b64 = (str) => window.btoa(unescape(encodeURIComponent(str)));
        const b64_to_utf8 = (str) => decodeURIComponent(escape(window.atob(str)));

        // ================= راه اندازی (Init) =================
        async function init() {
            // تلاش برای لود فایل data.json از کنار فایل اصلی (در محیط GitHub Pages)
            try {
                // اضافه کردن timestamp برای جلوگیری از کش مرورگر
                const res = await fetch('data.json?t=' + new Date().getTime());
                if (res.ok) {
                    appData = await res.json();
                    console.log('Data loaded from data.json');
                } else {
                    throw new Error('data.json not found');
                }
            } catch (err) {
                console.log('Loading default INITIAL_DATA fallback');
                appData = JSON.parse(JSON.stringify(INITIAL_DATA));
            }

            renderCustomerView();
            loadGithubSettings();
        }

        // ================= رندر بخش مشتری =================
        function renderCustomerView() {
            // اطلاعات پایه
            document.title = "منوی " + appData.restaurant.name;
            document.getElementById('cv-name').textContent = appData.restaurant.name;
            document.getElementById('cv-desc').textContent = "به منوی دیجیتال ما خوش آمدید";
            document.getElementById('cv-logo').src = appData.restaurant.logo;
            
            document.getElementById('cv-phone').textContent = appData.restaurant.phone;
            document.getElementById('cv-phone-link').href = `tel:${appData.restaurant.phone}`;
            document.getElementById('cv-map').href = appData.restaurant.mapLink;
            
            document.getElementById('cv-footer-address').innerHTML = `<i class="fas fa-map-pin ml-1"></i> ${appData.restaurant.address}`;
            document.getElementById('cv-footer-hours').innerHTML = `<i class="fas fa-clock ml-1"></i> ${appData.restaurant.hours}`;

            // رندر تب‌های دسته بندی
            const catContainer = document.getElementById('cv-categories');
            let tabsHtml = `<li class="cursor-pointer select-none">
                <a onclick="filterCategory('all', this)" class="cat-tab inline-block px-5 py-2 rounded-full border border-persian-teal text-persian-teal bg-persian-teal/10 font-bold transition">همه موارد</a>
            </li>`;
            
            appData.categories.forEach(cat => {
                tabsHtml += `<li class="cursor-pointer select-none">
                    <a onclick="filterCategory('${cat.id}', this)" class="cat-tab inline-block px-5 py-2 rounded-full border border-gray-200 text-gray-600 hover:border-persian-teal hover:text-persian-teal transition">${cat.name}</a>
                </li>`;
            });
            catContainer.innerHTML = tabsHtml;

            // رندر آیتم‌ها
            renderItems('all');
        }

        function filterCategory(catId, element) {
            currentCategory = catId;
            
            // استایل تب فعال
            document.querySelectorAll('.cat-tab').forEach(el => {
                el.className = "cat-tab inline-block px-5 py-2 rounded-full border border-gray-200 text-gray-600 hover:border-persian-teal hover:text-persian-teal transition";
            });
            element.className = "cat-tab inline-block px-5 py-2 rounded-full border border-persian-teal text-persian-teal bg-persian-teal/10 font-bold transition";

            // تنظیم عنوان
            const titleEl = document.getElementById('current-category-title');
            if (catId === 'all') {
                titleEl.innerHTML = '<i class="fas fa-utensils"></i> <span>همه موارد</span>';
            } else {
                const catName = appData.categories.find(c => c.id === catId)?.name || '';
                titleEl.innerHTML = `<i class="fas fa-hashtag text-persian-tealDark"></i> <span>${catName}</span>`;
            }

            renderItems(catId);
        }

        function renderItems(catId) {
            const container = document.getElementById('cv-items-grid');
            container.innerHTML = '';

            let filteredItems = appData.items;
            if (catId !== 'all') {
                filteredItems = appData.items.filter(item => item.categoryId === catId);
            }

            if (filteredItems.length === 0) {
                container.innerHTML = '<div class="col-span-full text-center text-gray-400 py-8">آیتمی در این دسته یافت نشد.</div>';
                return;
            }

            filteredItems.forEach(item => {
                const stockOverlay = !item.inStock ? `<div class="absolute inset-0 bg-white/70 backdrop-blur-[2px] z-10 flex items-center justify-center rounded-xl"><span class="bg-red-600 text-white px-4 py-2 rounded font-bold shadow-lg transform -rotate-12 border-2 border-white text-lg">ناموجود</span></div>` : '';
                const tagHtml = item.tags ? `<span class="absolute top-2 right-2 bg-persian-gold text-white text-xs font-bold px-2 py-1 rounded shadow">${item.tags}</span>` : '';
                const grayImgClass = !item.inStock ? 'grayscale opacity-60' : '';

                const cardHtml = `
                    <div class="bg-white rounded-xl shadow-sm hover:shadow-md transition border border-gray-100 flex p-3 relative h-36">
                        ${stockOverlay}
                        <div class="w-32 h-full flex-shrink-0 relative rounded-lg overflow-hidden border border-gray-100 shadow-inner">
                            <img src="${item.image || 'https://via.placeholder.com/150'}" alt="${item.name}" class="w-full h-full object-cover ${grayImgClass}">
                            ${tagHtml}
                        </div>
                        <div class="mr-4 flex flex-col justify-between flex-grow">
                            <div>
                                <h3 class="font-bold text-gray-800 text-base md:text-lg">${item.name}</h3>
                                <p class="text-xs text-gray-500 mt-1 line-clamp-2 leading-relaxed">${item.desc}</p>
                            </div>
                            <div class="text-left mt-2">
                                <span class="text-persian-teal font-bold text-lg">${formatPrice(item.price)}</span>
                                <span class="text-xs text-gray-500 mr-1">تومان</span>
                            </div>
                        </div>
                    </div>
                `;
                container.insertAdjacentHTML('beforeend', cardHtml);
            });
        }

        // رویداد مخفی باز کردن ادمین با دوبار کلیک روی هدر/لوگو
        document.getElementById('admin-trigger').addEventListener('dblclick', () => {
            document.getElementById('login-modal').classList.remove('hide');
        });

        // ================= رزرو =================
        function submitReservation(e) {
            e.preventDefault();
            alert("درخواست رزرو شما ثبت شد. (این یک دمو است و پیامی ارسال نشد)");
            e.target.reset();
        }

        // ================= بخش مدیریت (Admin Logic) =================
        function handleLogin() {
            const user = document.getElementById('login-user').value;
            const pass = document.getElementById('login-pass').value;
            // ورود ساده برای محیط سمت کلاینت (امنیت اصلی روی Token گیت‌هاب است)
            if (user === 'admin' && pass === 'admin') {
                document.getElementById('login-modal').classList.add('hide');
                openAdminPanel();
            } else {
                alert('نام کاربری یا رمز عبور اشتباه است.');
            }
        }

        function closeLogin() {
            document.getElementById('login-modal').classList.add('hide');
        }

        function exitAdmin() {
            document.getElementById('admin-view').classList.add('hide');
            // رفرش کردن ویو کاستومر با دیتای جدید در صورت تغییر محلی
            renderCustomerView();
        }

        function openAdminPanel() {
            document.getElementById('admin-view').classList.remove('hide');
            
            // پر کردن فرم اطلاعات پایه
            document.getElementById('admin-name').value = appData.restaurant.name;
            document.getElementById('admin-logo').value = appData.restaurant.logo;
            document.getElementById('admin-phone').value = appData.restaurant.phone;
            document.getElementById('admin-hours').value = appData.restaurant.hours;
            document.getElementById('admin-address').value = appData.restaurant.address;
            document.getElementById('admin-map').value = appData.restaurant.mapLink;

            renderAdminCategories();
            renderAdminItems();
        }

        function switchAdminTab(tabId) {
            document.querySelectorAll('.admin-tab-content').forEach(el => el.classList.add('hide'));
            document.querySelectorAll('.admin-tab-btn').forEach(el => {
                el.classList.remove('active', 'text-persian-teal', 'border-persian-teal', 'bg-gray-50');
                el.classList.add('text-gray-600', 'border-transparent');
            });
            
            document.getElementById('tab-' + tabId).classList.remove('hide');
            const btn = document.querySelector(`.admin-tab-btn[data-tab="${tabId}"]`);
            btn.classList.add('active', 'text-persian-teal', 'border-persian-teal', 'bg-gray-50');
            btn.classList.remove('text-gray-600', 'border-transparent');
        }

        function updateInfo(key, value) {
            appData.restaurant[key] = value;
        }

        // --- مدیریت دسته‌ها ---
        function renderAdminCategories() {
            const list = document.getElementById('admin-cat-list');
            list.innerHTML = '';
            appData.categories.forEach(cat => {
                list.innerHTML += `
                    <li class="flex justify-between items-center bg-gray-50 p-3 rounded border">
                        <span class="font-medium">${cat.name}</span>
                        <button onclick="deleteCategory('${cat.id}')" class="text-red-500 hover:text-red-700 p-1"><i class="fas fa-trash"></i></button>
                    </li>
                `;
            });
            
            // آپدیت دراپ‌دان مودال آیتم‌ها
            const select = document.getElementById('edit-item-cat');
            select.innerHTML = '';
            appData.categories.forEach(cat => {
                select.innerHTML += `<option value="${cat.id}">${cat.name}</option>`;
            });
        }

        function addCategory() {
            const name = document.getElementById('new-cat-name').value.trim();
            if (name) {
                appData.categories.push({ id: 'cat-' + generateId(), name });
                document.getElementById('new-cat-name').value = '';
                renderAdminCategories();
            }
        }

        function deleteCategory(id) {
            if (confirm('آیا مطمئن هستید؟ با حذف دسته، آیتم‌های داخل آن بی‌دسته می‌شوند.')) {
                appData.categories = appData.categories.filter(c => c.id !== id);
                renderAdminCategories();
                renderAdminItems(); // برای آپدیت نام دسته‌ها در لیست
            }
        }

        // --- مدیریت آیتم‌ها ---
        function renderAdminItems() {
            const tbody = document.getElementById('admin-items-list');
            tbody.innerHTML = '';
            
            appData.items.forEach(item => {
                const catName = appData.categories.find(c => c.id === item.categoryId)?.name || '<span class="text-red-400">بدون دسته</span>';
                const stockBadge = item.inStock 
                    ? '<span class="bg-green-100 text-green-800 text-xs px-2 py-1 rounded">موجود</span>' 
                    : '<span class="bg-red-100 text-red-800 text-xs px-2 py-1 rounded">ناموجود</span>';
                
                tbody.innerHTML += `
                    <tr class="border-b hover:bg-gray-50">
                        <td class="px-4 py-2">
                            <img src="${item.image}" class="w-10 h-10 rounded object-cover border">
                        </td>
                        <td class="px-4 py-2 font-medium text-gray-900">${item.name}</td>
                        <td class="px-4 py-2">${catName}</td>
                        <td class="px-4 py-2 dir-ltr text-right">${formatPrice(item.price)}</td>
                        <td class="px-4 py-2">${stockBadge}</td>
                        <td class="px-4 py-2 text-center space-x-2 space-x-reverse">
                            <button onclick="editItem('${item.id}')" class="text-blue-500 hover:text-blue-700 bg-blue-50 px-2 py-1 rounded"><i class="fas fa-edit"></i></button>
                            <button onclick="deleteItem('${item.id}')" class="text-red-500 hover:text-red-700 bg-red-50 px-2 py-1 rounded"><i class="fas fa-trash"></i></button>
                        </td>
                    </tr>
                `;
            });
        }

        function openItemModal() {
            document.getElementById('edit-item-id').value = '';
            document.getElementById('item-modal-title').innerText = 'افزودن آیتم جدید';
            
            document.getElementById('edit-item-name').value = '';
            document.getElementById('edit-item-price').value = '';
            document.getElementById('edit-item-desc').value = '';
            document.getElementById('edit-item-image').value = '';
            document.getElementById('edit-item-tag').value = '';
            document.getElementById('edit-item-stock').checked = true;
            if(appData.categories.length > 0){
                document.getElementById('edit-item-cat').value = appData.categories[0].id;
            }

            document.getElementById('item-modal').classList.remove('hide');
        }

        function editItem(id) {
            const item = appData.items.find(i => i.id === id);
            if (!item) return;

            document.getElementById('item-modal-title').innerText = 'ویرایش آیتم';
            document.getElementById('edit-item-id').value = item.id;
            document.getElementById('edit-item-name').value = item.name;
            document.getElementById('edit-item-cat').value = item.categoryId;
            document.getElementById('edit-item-price').value = item.price;
            document.getElementById('edit-item-desc').value = item.desc || '';
            document.getElementById('edit-item-image').value = item.image || '';
            document.getElementById('edit-item-tag').value = item.tags || '';
            document.getElementById('edit-item-stock').checked = item.inStock;

            document.getElementById('item-modal').classList.remove('hide');
        }

        function closeItemModal() {
            document.getElementById('item-modal').classList.add('hide');
        }

        function saveItem() {
            const id = document.getElementById('edit-item-id').value;
            const itemObj = {
                id: id || ('item-' + generateId()),
                name: document.getElementById('edit-item-name').value,
                categoryId: document.getElementById('edit-item-cat').value,
                price: parseInt(document.getElementById('edit-item-price').value) || 0,
                desc: document.getElementById('edit-item-desc').value,
                image: document.getElementById('edit-item-image').value,
                tags: document.getElementById('edit-item-tag').value,
                inStock: document.getElementById('edit-item-stock').checked
            };

            if (!itemObj.name) {
                alert('نام آیتم الزامی است.');
                return;
            }

            if (id) {
                // ویرایش
                const index = appData.items.findIndex(i => i.id === id);
                if (index > -1) appData.items[index] = itemObj;
            } else {
                // افزودن جدید
                appData.items.push(itemObj);
            }

            closeItemModal();
            renderAdminItems();
        }

        function deleteItem(id) {
            if (confirm('آیا از حذف این آیتم مطمئن هستید؟')) {
                appData.items = appData.items.filter(i => i.id !== id);
                renderAdminItems();
            }
        }

        // ================= گیت‌هاب (GitHub API) =================
        function loadGithubSettings() {
            document.getElementById('gh-user').value = localStorage.getItem('menu_gh_user') || '';
            document.getElementById('gh-repo').value = localStorage.getItem('menu_gh_repo') || '';
            document.getElementById('gh-pat').value = localStorage.getItem('menu_gh_pat') || '';
        }

        function saveGithubSettings() {
            localStorage.setItem('menu_gh_user', document.getElementById('gh-user').value.trim());
            localStorage.setItem('menu_gh_repo', document.getElementById('gh-repo').value.trim());
            localStorage.setItem('menu_gh_pat', document.getElementById('gh-pat').value.trim());
            alert('تنظیمات گیت‌هاب روی مرورگر شما ذخیره شد.');
        }

        async function saveToGithub() {
            const user = localStorage.getItem('menu_gh_user');
            const repo = localStorage.getItem('menu_gh_repo');
            const pat = localStorage.getItem('menu_gh_pat');

            if (!user || !repo || !pat) {
                alert('لطفاً ابتدا تنظیمات گیت‌هاب را در تب مربوطه کامل کنید.');
                switchAdminTab('github');
                return;
            }

            const btn = document.querySelector('header button.bg-green-600');
            const originalText = btn.innerHTML;
            btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> <span>در حال ذخیره...</span>';
            btn.disabled = true;

            const apiUrl = `https://api.github.com/repos/${user}/${repo}/contents/data.json`;
            const contentJsonStr = JSON.stringify(appData, null, 2);
            const contentBase64 = utf8_to_b64(contentJsonStr);

            try {
                // 1. دریافت SHA فعلی فایل (برای آپدیت اجباری است)
                let sha = '';
                const getRes = await fetch(apiUrl, {
                    headers: { 
                        'Authorization': `token ${pat}`,
                        'Accept': 'application/vnd.github.v3+json'
                    }
                });

                if (getRes.ok) {
                    const fileData = await getRes.json();
                    sha = fileData.sha;
                }

                // 2. ارسال فایل جدید
                const putRes = await fetch(apiUrl, {
                    method: 'PUT',
                    headers: {
                        'Authorization': `token ${pat}`,
                        'Accept': 'application/vnd.github.v3+json',
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({
                        message: 'Update menu data via Admin Panel',
                        content: contentBase64,
                        sha: sha || undefined // اگر فایل نبود نیازی به sha نیست (ساخت فایل جدید)
                    })
                });

                if (putRes.ok) {
                    alert('✅ تغییرات با موفقیت در مخزن گیت‌هاب ثبت شد!\n\nنکته: ممکن است ۱ تا ۳ دقیقه طول بکشد تا GitHub Pages به‌روزرسانی شود.');
                } else {
                    const err = await putRes.json();
                    throw new Error(err.message || 'خطای نامشخص');
                }
            } catch (error) {
                console.error(error);
                alert('❌ خطا در ارتباط با گیت‌هاب:\n' + error.message + '\n\nلطفاً مطمئن شوید اطلاعات PAT و نام مخزن صحیح است.');
            } finally {
                btn.innerHTML = originalText;
                btn.disabled = false;
            }
        }

        // شروع برنامه
        window.addEventListener('DOMContentLoaded', init);

    </script>
</body>
</html>
